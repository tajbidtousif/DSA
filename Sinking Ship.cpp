#include<bits/stdc++.h>
using namespace std;

int main()
{
    int tc;
    cin >> tc;

    while(tc--)
    {
        string str;
        cin >> str;
        for(int i=0; i<str.size(); i++)
        {
            if(isalpha(str[i]))
            {
                continue;
            }
            else{
                if(str[i+1]=='r')
            }
        }

    }
}
